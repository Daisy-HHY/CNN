根据作业要求，你们小组有3个人，任务主要分为三个核心部分：SVM算法实现、神经网络M模型实现与蒸馏、以及最终的报告整合与理论讲解。

由于不需要上台汇报，重点在于代码实现的完整性和Word报告的逻辑性。以下是具体的分工建议：

成员A：传统机器学习担当（SVM部分）
核心任务：负责PPT中“1.采用SVM对cifar-10数据集进行分类”的部分。

- 具体工作：
    - 数据预处理：编写代码加载CIFAR-10，并进行SVM所需的特征提取（例如使用HOG特征或PCA降维，因为直接跑原始像素SVM太慢且效果差）。
    - 模型训练：分别实现“无核函数（线性核）”和“有核函数（如RBF核）”的SVM分类器。
    - 结果记录：记录两种情况下的准确率、训练时间。
    - 撰写报告第一部分：描述SVM的原理，展示特征提取的效果，对比有无核函数的实验结果（表格+分析）。

成员B：深度学习担当（教师模型M & 蒸馏）
核心任务：负责PPT中“2.选用神经网络M”和“3.蒸馏学习”的部分。这是工作量最大的一块。

- 具体工作：
    - 构建教师模型M：搭建一个性能较好的CNN（比如VGG或者简单的ResNet），在CIFAR-10上训练，作为“教师”。
    - 构建学生模型：搭建一个较小的ResNet18（或者比教师模型更小的网络）。
    - 蒸馏实现：编写蒸馏损失函数（Distillation Loss = Hard Loss + Soft Loss * Temperature²），训练学生模型。
    - 撰写报告第二、三部分：展示教师模型的架构，详细解释蒸馏过程（温度T的选择等），对比“学生模型直接训练”vs“学生模型蒸馏训练”的精度差异。

成员C：项目经理 & 理论专家（统筹与理论）
核心任务：负责PPT中“汇报两组各负责一个算法讲解”的理论部分，以及最终Word报告的统稿。

- 具体工作：
    - 理论调研：
        - 查找并整理SMO算法（序列最小优化算法，SVM的核心求解算法）的数学原理和流程图。
        - 查找并整理神经网络通用逼近定理（Universal Approximation Theorem）的证明思路或直观解释。
    - 报告整合：将A和B的代码结果、截图汇总到Word文档中。
    - 排版与润色：按照“数据展示-预处理-特征工程-模型选取-评估”的标准提纲，把大家的散乱内容串联成一篇逻辑通顺的报告。确保格式美观。

分工协作流程图

graph LR
    A[成员A: SVM实验] --> C[成员C: 报告整合]
    B[成员B: 神经网络&蒸馏实验] --> C
    D[成员C: SMO/逼近定理理论] --> C
    C --> E[最终Word报告]

给小组的建议（避坑指南）

1. 关于SVM（成员A）：CIFAR-10图片是32x32的彩色图，直接丢进SVM会非常慢。一定要做特征提取（比如转灰度、HOG特征）或者降维（PCA），否则跑一天都跑不完。
2. 关于蒸馏（成员B）：蒸馏的关键在于温度参数（Temperature）的调节。建议在报告中多放几组不同温度下的对比实验数据，这样报告内容会很充实，显得工作量大。
3. 关于理论（成员C）：SMO算法比较枯燥，建议找一些图解或者动画截图放在报告里，不要只放公式。

这样分工，每个人都有明确的产出，最后由C同学统一格式，效率最高。


这是一个完整的实验报告的所有要求  我的任务目标是只需要完成成员B的内容 其他两个成员的任务不需要管
先针对我的任务进行规划  做完所有规划之后再进行代码执行